services:
  financial-planner:
    build: .
    container_name: financial-planner
    ports:
      - "127.0.0.1:3210:3210"
    volumes:
      - ./data:/app/data
    environment:
      - DATA_DIR=/app/data
      - ALLOWED_HOSTS=${ALLOWED_HOSTS:-}
    restart: unless-stopped
